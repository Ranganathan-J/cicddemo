[tool.black]
line-length = 88
target-version = ["py311"]
include = '\.pyi?$'

[tool.isort]
profile = "black"          # makes isort compatible with Black
line_length = 88
known_first_party = ["calculator"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
